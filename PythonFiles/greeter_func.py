def greet_user(username):
    """Display a simple greeting.
       This is called docstring, which describes what the function does. 
    """
    print(f"Hello, {username.title()}")

greet_user('jesse')