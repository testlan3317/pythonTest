# sorting means put elements in an ordered sequence
# ordered sequence is any sequence that has an order corresponding to elements, like numeric or alpabetical, ascending or descending.

import numpy as np

arr = np.array([3, 2, 0, 1])

print(np.sort(arr))
